const EventController = () => {
  return (
    <div>
      {/*
        include the same list of events as on advanced scoreboard controller to update/remove past events
        create simple UI to easily capture events and statistics
        need to decide how to capture statistics such as shooting percentage
        probably create new table statistics which would include references to match, team and player
        then create columns for:
        shots attempted and shots scored (could be separated to total and by type as well)
        interceptions and losses
        rebounds in offense and in defense (both successful and unsuccessful)
        play time

        create page for displaying events, on this page create control to display an event or statistic
      */}
      <div>event controller</div>
    </div>
  );
};

export default EventController;
